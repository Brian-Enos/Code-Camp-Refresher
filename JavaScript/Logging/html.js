

/* 
You can log any element which exists within the DOM(Document Object Model).
This can include but not limited to: body, html, p, section etc of the html document
See the examples below
*/
console.log(document.body);
console.log(document.h1);
console.log(document.p);

//etc etc 